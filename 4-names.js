//local
const secret = 'SUPER SECRET'
//share
const ansh = 'ansh'
const john = 'john'

// console.log(module)
// the above command shows us that every file is module by default and we can decide what to export and all
// a module is basically a class and our files are it's objects

module.exports = {john, ansh} // this would be available

// By default nothing is available