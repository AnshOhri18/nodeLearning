// A basic function with an argument
const sayHi = (name) => {
    console.log(`Hello there ${name}`)
}

// to add a variable in a function write ${var}

module.exports = sayHi // exporting the functions.